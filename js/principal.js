
var xba = document.querySelector("#escolhaServico");
var escolhaServico = addEventListener('input', optcheck());
	   

function optcheck(e) {
	alert("Got here");


function alerta(e) {
	window.alert("Deu certuuuuuu");
}





/** function tornar visivel () {
	
	id xxx                   remover class invisivel
	id="parte_comum_final"   remover class invisivel
}

function tonar invisível () {
	
	id xxx                   add class invisivel
	id="parte_comum_final"   add class invisivel
	
} **/


/**
var titulo = document.querySelector(".titulo");
titulo.textContent = "Aparecida Nutricionista";

var pacientes = document.querySelectorAll(".paciente");

for (var i = 0; i < pacientes.length; i++) {

    var paciente = pacientes[i];

    var tdPeso = paciente.querySelector(".info-peso");
    var peso = tdPeso.textContent;

    var tdAltura = paciente.querySelector(".info-altura");
    var altura = tdAltura.textContent;

    var tdImc = paciente.querySelector(".info-imc");

    var pesoEhValido = true;
    var alturaEhValida = true;

    if (peso <= 0 || peso >= 1000) {
        console.log("Peso inválido!");
        pesoEhValido = false;
        tdImc.textContent = "Peso inválido";
        paciente.classList.add("paciente-invalido");
    }

    if (altura <= 0 || altura >= 3.00) {
        console.log("Altura inválida!");
        alturaEhValida = false;
        tdImc.textContent = "Altura inválida";
        paciente.classList.add("paciente-invalido");
    }

    if (pesoEhValido && alturaEhValida) {
        var imc = peso / (altura * altura);
        tdImc.textContent = imc.toFixed(2);
    }
}

*/